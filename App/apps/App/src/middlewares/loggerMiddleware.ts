// import { PURGE } from 'redux-persist';

// const purgeMiddleware = (store:any) => (next:any) => (action:any) => {
//   if (action.type === PURGE) {
//     console.log('Purging state...');
//     store.dispatch({ type: 'CLEAR_STATE' })
//   }
//   return next(action);
// };

// export default purgeMiddleware;
